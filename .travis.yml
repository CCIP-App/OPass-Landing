os: linux
dist: focal
language: go
go:
  - 1.14.x
git:
  depth: 1
branches:
  only:
  - master
env:
  - GO111MODULE=on
script:
  - ./build.sh
deploy:
  strategy: git
  provider: pages
  skip_cleanup: true
  token: $GITHUB_TOKEN
  fqdn: opass.app
  local_dir: .build/
  on:
    branch: master
